The file debug.keystore can be used to test drive the jarsigning process.

The build server's HEAD job uses this key to build the package.


Keystore password: keystore

Key password: keypass

Alias: chinarewards
